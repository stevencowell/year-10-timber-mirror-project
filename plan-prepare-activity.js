(() => {
  'use strict';
  const key = 'mirror-plan-prepare-activity-v1';
  const questions = [
    { question: 'Why must you use written dimensions and notes instead of measuring a drawing on a screen?', options: ['The drawing may have been enlarged or reduced, so the picture is not a reliable measuring scale.', 'A ruler cannot be used in a timber workshop.', 'The mirror frame does not need accurate dimensions.', 'Screen measurements are always more accurate than written dimensions.'], correct: 0, hint: 'Think about what can happen when an image is printed, copied or displayed on a different screen.', strongHint: 'The picture can change size. What information stays accurate even when it does?', feedback: ['Correct. Written dimensions and drawing notes remain authoritative when an image is resized.', 'A ruler is useful in the workshop, but it would only measure the resized image on the screen.', 'Accurate dimensions are essential for a square frame and a mirror that fits safely.', 'Written dimensions are the reliable information; the displayed picture may not be full scale.'] },
    { question: 'What is the best reason for preparing a cutting list before cutting timber?', options: ['It removes the need to read the drawing.', 'It identifies the components, quantities and finished sizes so material can be planned accurately.', 'It guarantees that no timber will need sanding.', 'It lets students choose any dimension they prefer.'], correct: 1, hint: 'A cutting list helps you account for every part before material is removed.', strongHint: 'Look for the answer that connects component names, quantities and finished sizes.', feedback: ['The drawing still controls the work. The cutting list helps organise that information.', 'Correct. A cutting list helps prevent missed parts, incorrect sizes and unnecessary waste.', 'Sanding is still part of the finish process.', 'The supplied drawing controls the required sizes and details.'] },
    { question: 'Why should a mirror frame be dry-fitted and checked for square before glue is applied?', options: ['To make the glue dry faster.', 'To check the joint fit and square before the assembly becomes permanent.', 'To avoid measuring the mirror opening.', 'To change the type of timber being used.'], correct: 1, hint: 'A dry fit is a chance to find a problem while it can still be corrected.', strongHint: 'Once glue is applied, adjustment is much harder. What should be checked first?', feedback: ['Glue drying time is not the purpose of a dry fit.', 'Correct. A dry fit lets you correct fit or square problems before glue makes the assembly permanent.', 'The mirror opening still needs to be checked against the plan and supplied mirror.', 'A dry fit does not change the material.'] }
  ];
  const writing = { title: 'Explain a planning decision', prompt: 'Explain how careful planning will help you produce a safe, accurate and well-finished timber mirror.', scaffold: ['One important project requirement is…', 'Before cutting, I would…', 'This improves safety or quality because…', 'For example, the drawing shows…', 'As a result, the finished mirror will…'], model: 'Careful planning helps me make the mirror safely and accurately because the project drawing controls the required size, joints and mirror opening. Before cutting, I would read the written dimensions, prepare a cutting list and inspect the timber for defects. I would mark matching parts from the same datum so the frame members stay consistent. I would then dry-fit the joints and check the frame for square before applying glue. This reduces waste, prevents a poor mirror fit and gives me a better chance of producing a strong, neat finished frame.' };
  let state = load();
  function fresh() { return { first: '', last: '', studentClass: '', mc: {}, written: { response: '', checked: false, model: false, score: null, guidance: '' } }; }
  function load() { try { return { ...fresh(), ...JSON.parse(localStorage.getItem(key) || '{}') }; } catch { return fresh(); } }
  function save(message = 'Saved') { localStorage.setItem(key, JSON.stringify(state)); const s = document.getElementById('save-status'); if (s) s.textContent = message; update(); }
  const escape = value => String(value).replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char]));
  const words = value => String(value || '').trim().split(/\s+/).filter(Boolean).length;
  function renderMc() { const el = document.getElementById('mc-questions'); if (!el) return; el.innerHTML = questions.map((item, i) => { const saved = state.mc[i] || {}; const opts = item.options.map((option, j) => `<label class="mirror-option ${saved.mastered && j === item.correct ? 'correct' : ''} ${!saved.mastered && saved.wrong === j ? 'wrong' : ''}"><input type="radio" name="mc-${i}" value="${j}" ${saved.selected === j ? 'checked' : ''}><span>${escape(option)}</span></label>`).join(''); return `<article class="mirror-question-card ${saved.mastered ? 'mastered' : ''}" data-index="${i}"><h3><span>${i + 1}</span>${escape(item.question)}</h3><div class="mirror-options">${opts}</div><div class="mirror-actions"><button class="mirror-primary-button" data-action="check" type="button">${saved.mastered ? 'Check again' : 'Check answer'}</button><button class="mirror-secondary-button" data-action="hint" type="button">${saved.hint ? 'Hide hint' : 'Need a hint?'}</button><em>${saved.mastered ? 'Mastered' : `Attempts: ${saved.attempts || 0}`}</em></div>${saved.hint ? `<div class="mirror-hint"><strong>Hint:</strong> ${escape((saved.attempts || 0) >= 2 ? item.strongHint : item.hint)}</div>` : ''}${saved.feedback ? `<div class="mirror-feedback ${saved.feedbackType}">${saved.feedback}</div>` : ''}</article>`; }).join(''); }
  function renderWritten() { const el = document.getElementById('written-questions'); if (!el) return; const s = state.written; el.innerHTML = `<article class="mirror-written-card ${s.checked ? 'reviewed' : ''}"><h3>${writing.title}</h3><p>${writing.prompt}</p><div class="mirror-scaffold"><strong>Sentence starters</strong><ul>${writing.scaffold.map(x => `<li>${x}</li>`).join('')}</ul></div><textarea id="written-response" placeholder="Write your response here before viewing the model answer…">${escape(s.response)}</textarea><div class="mirror-response-meta"><span>Word count: ${words(s.response)} words</span><span>${s.checked ? 'Guidance reviewed' : 'Not yet reviewed'}</span></div><div class="mirror-actions"><button class="mirror-primary-button" data-action="check-written" type="button">Check my response</button><button class="mirror-secondary-button" data-action="model" type="button">${s.model ? 'Hide model response' : 'Compare with model response'}</button></div>${s.guidance ? `<div class="mirror-feedback ${s.ready ? 'correct' : 'not-yet'}">${s.guidance}</div>` : ''}${s.model ? `<div class="mirror-model"><strong>Model response:</strong> ${writing.model}<div class="mirror-score"><strong>Self-assess:</strong>${[3,2,1,0].map(n => `<button type="button" data-score="${n}" class="mirror-score-button ${s.score === n ? 'selected' : ''}">${n}/3</button>`).join('')}</div></div>` : ''}</article>`; }
  function update() { const mastered = questions.filter((_, i) => state.mc[i]?.mastered).length; const reviewed = state.written.checked ? 1 : 0; const details = Boolean(state.first.trim() && state.last.trim() && state.studentClass.trim()); [['mc-progress-number', `${mastered}/3`], ['written-progress-number', `${reviewed}/1`], ['summary-mc', `${mastered}/3`], ['summary-written', `${reviewed}/1`], ['summary-details', details ? 'Complete' : 'Incomplete']].forEach(([id, value]) => { const el = document.getElementById(id); if (el) el.textContent = value; }); }
  function pdf() { const line = text => String(text || '').replace(/[()\\]/g, '\\$&').replace(/[^\x20-\x7E]/g, '?'); const entries = [`Mirror Project - Plan and Prepare`, `Student: ${state.first} ${state.last}`.trim(), `Class: ${state.studentClass || 'Not provided'}`, `Downloaded: ${new Date().toLocaleDateString('en-AU')}`, '', 'Knowledge checks']; questions.forEach((q, i) => { entries.push(`${i + 1}. ${q.question}`); entries.push(`Response: ${state.mc[i]?.selected >= 0 ? q.options[state.mc[i].selected] : 'No answer recorded'}`); }); entries.push('', 'Guided written response', writing.prompt, `Student response: ${state.written.response || 'No response recorded.'}`, `Self-assessment: ${Number.isInteger(state.written.score) ? `${state.written.score}/3` : 'Not selected'}`); const stream = entries.flatMap((entry, i) => [`BT /F1 ${i === 0 ? 16 : 10} Tf 45 ${800 - i * 15} Td (${line(entry)}) Tj ET`]).join('\n'); const pdfText = `%PDF-1.4\n1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n2 0 obj\n<< /Type /Pages /Kids [3 0 R] /Count 1 >>\nendobj\n3 0 obj\n<< /Type /Page /Parent 2 0 R /MediaBox [0 0 595 842] /Resources << /Font << /F1 4 0 R >> >> /Contents 5 0 R >>\nendobj\n4 0 obj\n<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>\nendobj\n5 0 obj\n<< /Length ${stream.length} >>\nstream\n${stream}\nendstream\nendobj\ntrailer\n<< /Root 1 0 R >>\n%%EOF`; const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([pdfText], { type: 'application/pdf' })); a.download = `${(state.first || 'student').replace(/\W+/g, '-')}-mirror-plan-and-prepare.pdf`; a.click(); setTimeout(() => URL.revokeObjectURL(a.href), 1000); }
  document.addEventListener('DOMContentLoaded', () => { ['first','last','studentClass'].forEach((field, i) => { const el = document.getElementById(['student-first-name','student-last-name','student-class'][i]); el.value = state[field]; el.addEventListener('input', () => { state[field] = el.value; save('Details saved'); }); }); renderMc(); renderWritten(); update(); document.getElementById('mc-questions').addEventListener('change', e => { if (!e.target.matches('input')) return; const card = e.target.closest('[data-index]'); state.mc[card.dataset.index] = { ...(state.mc[card.dataset.index] || {}), selected: Number(e.target.value) }; save('Draft saved'); }); document.getElementById('mc-questions').addEventListener('click', e => { const card = e.target.closest('[data-index]'); if (!card) return; const i = Number(card.dataset.index), item = questions[i], saved = state.mc[i] || {}; if (e.target.dataset.action === 'hint') { saved.hint = !saved.hint; state.mc[i] = saved; save(); renderMc(); } if (e.target.dataset.action === 'check') { saved.attempts = (saved.attempts || 0) + 1; if (!Number.isInteger(saved.selected)) { saved.feedbackType = 'not-yet'; saved.feedback = '<strong>Choose an answer first.</strong> Then use the feedback to improve.'; } else if (saved.selected === item.correct) { saved.mastered = true; saved.wrong = null; saved.feedbackType = 'correct'; saved.feedback = `<strong>Correct.</strong> ${item.feedback[saved.selected]}`; } else { saved.mastered = false; saved.wrong = saved.selected; saved.hint = true; saved.feedbackType = 'not-yet'; saved.feedback = `<strong>Not yet.</strong> ${item.feedback[saved.selected]} Read the hint and try again.`; } state.mc[i] = saved; save(); renderMc(); } }); document.getElementById('written-questions').addEventListener('input', e => { if (e.target.id !== 'written-response') return; state.written.response = e.target.value; state.written.checked = false; state.written.guidance = ''; save('Draft saved'); renderWritten(); }); document.getElementById('written-questions').addEventListener('click', e => { if (e.target.dataset.action === 'check-written') { const wc = words(state.written.response); state.written.checked = true; state.written.ready = wc >= 15; state.written.guidance = wc >= 15 ? '<strong>Good start.</strong> Compare your ideas with the model response, then improve any unclear wording.' : `<strong>Good start.</strong> Add about ${15 - wc} more words for a short attempt, or compare with the model response after this check.`; save(); renderWritten(); } if (e.target.dataset.action === 'model') { const wc = words(state.written.response); if (!state.written.model && wc < 15 && !state.written.checked) { state.written.checked = true; state.written.guidance = '<strong>Start with a short attempt.</strong> Write about 15 words, or use Check my response once, then compare your ideas with the model response.'; } else { state.written.model = !state.written.model; } save(); renderWritten(); } if (e.target.dataset.score) { state.written.score = Number(e.target.dataset.score); save(); renderWritten(); } }); document.getElementById('download-pdf-button').addEventListener('click', pdf); document.getElementById('reset-lesson-button').addEventListener('click', () => { if (confirm('Reset this topic on this browser?')) { state = fresh(); save('Topic reset'); location.reload(); } }); });
})();
